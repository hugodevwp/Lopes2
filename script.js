const header=document.querySelector('.header'),menu=document.querySelector('.menu-btn');menu.addEventListener('click',()=>{const open=header.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.header nav a').forEach(a=>a.addEventListener('click',()=>header.classList.remove('open')));const tabs=document.querySelectorAll('.service-tabs button'),cards=document.querySelectorAll('.service-card[data-category]');tabs.forEach(tab=>tab.addEventListener('click',()=>{tabs.forEach(t=>t.classList.remove('active'));tab.classList.add('active');cards.forEach(card=>card.classList.toggle('hidden',tab.dataset.filter!=='todos'&&card.dataset.category!==tab.dataset.filter))}));document.getElementById('year').textContent=new Date().getFullYear();
